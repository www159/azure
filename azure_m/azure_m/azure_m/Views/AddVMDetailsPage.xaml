<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="azure_m.Views.AddVMDetailsPage"
             xmlns:viewmodels="clr-namespace:azure_m.ViewModels"
             xmlns:views="clr-namespace:azure_m.Views.VMDetails"
             >

    <ContentPage.BindingContext>
        <viewmodels:VMDetailsViewModel/>
    </ContentPage.BindingContext>
    <StackLayout>
        <CarouselView  ItemsSource="{Binding pages}">
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <ContentView Content="{Binding Content}"></ContentView>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>
    </StackLayout>
    
</ContentPage>